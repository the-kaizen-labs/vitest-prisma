---
'vitest-environment-prisma-tx': patch
---

Bump dev dependencies (`prisma` 7.2.0 → 7.8.0, `vitest` + `@vitest/coverage-v8` 4.0.16 → 4.1.5, `tsdown` 0.21.7 → 0.21.10) to clear 14 transitive Dependabot alerts (hono, vite, effect, defu, picomatch, lodash). No runtime or peer-dependency changes.
